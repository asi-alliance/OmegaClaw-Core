# Override: grant the agent full internet access for web search, RAG, etc.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.full-network.yml up -d
#
# API keys remain hidden in the proxy -- this only opens outbound networking.

services:
  omegaclaw:
    networks:
      - internal
      - external
